spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:postgresql://localhost:1234/tasklist?currentSchema=tasklist
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      continue-on-error: false
    #  data-locations: classpath:demo_data.sql
security:
  jwt:
    secret: 2��km6
    access: 3600000
    refresh: 2592000000

logging:
  level:
    root: WARN



